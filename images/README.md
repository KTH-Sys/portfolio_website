# Images

Drop your screenshots and photo here using these **exact filenames** and the site picks
them up automatically. Until a file exists, a styled gray placeholder shows instead —
nothing breaks.

## Project screenshots (shown in the Work grid + as the list hover-preview)
| File | Project | Suggested |
|------|---------|-----------|
| `sportsmom.png`    | SportsMom                    | landscape ~1200×900, ≤500 KB |
| `skaler.png`       | Skaler                       | landscape ~1200×900, ≤500 KB |
| `barter.png`       | Barter                       | landscape ~1200×900, ≤500 KB |
| `davis-prices.png` | Davis House Price Prediction | landscape ~1200×900, ≤500 KB |
| `pomibear.png`     | pomibear!                    | landscape ~1200×900, ≤500 KB |

## Profile photo (About section)
| File | Notes |
|------|-------|
| `profile.jpg` | portrait, ~800×1000 (4:5), face roughly centered |

### Notes
- Filenames are referenced in `index.html` (project `<img src>` + `data-img`) and in the
  About `<img src>`. If you'd rather use `.jpg` for screenshots (or `.png` for the photo),
  change the extension in those two spots to match.
- Keep files small (compress them) so the page loads fast.
